import React from "react";
import ReactDOM from "react-dom/client";
import VvvSlotMachine from "./App";
import "./index.css"; // 必要なスタイル（Tailwind使うなら）

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <VvvSlotMachine />
  </React.StrictMode>
);
